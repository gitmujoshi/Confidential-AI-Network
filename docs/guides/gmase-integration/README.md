# Open-GMASE ↔ CAN integration screenshots

Captured by `frontend/tests/e2e/inference-deploy-ui.spec.js` when
`E2E_WAIT_FOR_LOCAL_TRAINING=true` and Open-GMASE OPA is running.

| File | What it shows |
| --- | --- |
| `01-tdc-deploy-inference.png` | Training → Deploy for inference (OPA authorize `deploy_inference`) |
| `02-tdc-inference-app.png` | Inference app ready |
| `03-tdc-inference-predict-gmase.png` | Prediction result with **Open-GMASE policy gate** ALLOW panel |

Synced into the blog as `docs/blogs/assets/gmase/` (see `pages-blogs.yml`).
