# End-to-End Lifecycle Guide — Onboard → Sign → Train

> Auto-generated by Playwright (`npm run test:e2e:lifecycle-guide` in `frontend/`).
> Screenshots refreshed: **2026-07-24**. Prefer regenerating over hand-editing images.

This guide walks through the full multi-party happy path in the local stack:

1. Onboard **TDC**, **TDP**, and **TSP/CCRP** participants
2. TDP publishes a dataset
3. TDC creates a Ricardian contract
4. TDP and TSP are notified, review, and sign
5. TDC views the **SIGNED** contract, runs local-docker training, and inspects **logs** + **provenance**

## Demo credentials

| Role | Notes |
|---|---|
| TDC / TDP / TSP | Fresh users registered during the guide run (emails in screenshots) |
| Password after first login | `TestNewPassword123!` |
| Local compute | TSP is configured with **Local** cloud provider for `TRAINING_EXECUTION_MODE=local-docker` |

## Happy path

### 1. Onboard TDC (register)

Each participant starts at **User Registration**. Select party type **TDC (Training Data Consumer)**, then register.

![Onboard TDC (register)](screenshots/01-onboard-tdc-register.png)

### 2. TDC first login dashboard

After first-login password setup, the TDC lands on their dashboard and can browse datasets / create contracts.

![TDC first login dashboard](screenshots/02-onboard-tdc-dashboard.png)

### 3. Onboard TDP (register)

Register a **TDP (Training Data Provider)** who will publish datasets and sign contracts that use them.

![Onboard TDP (register)](screenshots/03-onboard-tdp-register.png)

### 4. TDP first login dashboard

The TDP dashboard is the home for datasets and incoming signature requests.

![TDP first login dashboard](screenshots/04-onboard-tdp-dashboard.png)

### 5. TDP publishes a dataset

The TDP publishes a catalog dataset. TDCs can select it when creating a Ricardian contract.

![TDP publishes a dataset](screenshots/05-tdp-dataset-published.png)

### 6. Onboard TSP / CCRP (register)

Register a **TSP** (Tech Service Provider; also called CCRP in older docs) who hosts the training environment and co-signs the contract.

![Onboard TSP / CCRP (register)](screenshots/06-onboard-tsp-register.png)

### 7. TSP Local cloud readiness

Configure the TSP with a **Local** provider so contracts can run with `TRAINING_EXECUTION_MODE=local-docker` in this environment.

![TSP Local cloud readiness](screenshots/07-onboard-tsp-local.png)

### 8. TDC creates contract — details & dataset

The TDC selects the TDP dataset, AI model, price, duration, and terms.

![TDC creates contract — details & dataset](screenshots/08-tdc-create-details.png)

### 9. TDC creates contract — select TSP

Select the onboarded **TSP** (Local) and environment/KMS settings for the training session.

![TDC creates contract — select TSP](screenshots/09-tdc-create-tsp.png)

### 10. TDC creates contract — submit path

Complete environment/KMS settings and submit the wizard.
The contract enters **PENDING_TDP_APPROVAL** and notifies the TDP (then TSP after TDP signs).

![TDC creates contract — submit path](screenshots/10-tdc-create-submit.png)

### 11. TDP gets notified

The TDP sees signature / contract notifications for agreements that reference their datasets.

![TDP gets notified](screenshots/11-tdp-notifications.png)

### 12. TDP reviews and signs

On **Contract Details**, the TDP reviews terms and clicks **Sign Contract as TDP**. Status advances to **PENDING_TSP_APPROVAL**.

![TDP reviews and signs](screenshots/12-tdp-sign.png)

### 13. TSP / CCRP gets notified

The TSP is notified when a contract is waiting for clean-room provider signature.

![TSP / CCRP gets notified](screenshots/13-tsp-notifications.png)

### 14. TSP / CCRP reviews and signs

The TSP reviews environment commitments and signs. When TDP and TSP have both signed, status becomes **SIGNED**.

![TSP / CCRP reviews and signs](screenshots/14-tsp-sign.png)

### 15. TDC views the signed contract

The TDC opens the contract and confirms **SIGNED** (TDP + TSP signatures complete).

![TDC views the signed contract](screenshots/15-tdc-signed-contract.png)

### 16. TDC starts training

On **Training**, the TDC starts a job for the signed contract (`TRAINING_EXECUTION_MODE=local-docker` in local env).

![TDC starts training](screenshots/16-tdc-training-ready.png)

### 17. Training completed

When the local trainer finishes, the job status is **COMPLETED** with metrics and artifact actions.

![Training completed](screenshots/17-tdc-training-completed.png)

### 18. TDC views provenance report

**View job provenance** opens the host/API provenance bundle for the completed run (inputs, metrics, artifacts).

![TDC views provenance report](screenshots/18-tdc-provenance.png)

### 19. TDC views training run logs

**View logs** shows trainer/runner output captured for the local-docker job.

![TDC views training run logs](screenshots/19-tdc-training-logs.png)

## Related docs

- [Participant onboarding & E2E lifecycle (canonical text)](../PARTICIPANT_ONBOARDING_AND_E2E_LIFECYCLE.md)
- [Per-role screenshot guides](../role-user-guides/README.md)
- [Contract signing user guide](../../features/contract-signing/CONTRACT_SIGNING_USER_GUIDE.md)
- [Top-level user guide](../../USER_GUIDE.md)

## Regenerate

```bash
# Stack must be up: backend :5001, frontend :3000, Keycloak, Docker trainer image
cd frontend
npm run test:e2e:lifecycle-guide
```

Optional cleanup afterward (keeps seed users/catalog): `npm run cleanup:e2e-data` from repo root.
