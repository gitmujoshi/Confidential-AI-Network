# End-to-End Lifecycle Guide — Onboard → Sign → Train → Inference

> Auto-generated by Playwright (`npm run test:e2e:lifecycle-guide` in `frontend/`).
> Screenshots refreshed: **2026-08-14**. Prefer regenerating over hand-editing images.

This guide walks through the full multi-party happy path in the local stack:

1. **Enterprise-register** **TDC**, **TDP**, and **TSP/CCRP** (User Type = Enterprise + organization)
2. TDP publishes an NLP dataset (Hugging Face `ag_news` reference)
3. TDC creates a Ricardian contract with **PyTorch** / **DistilBERT** (quality demo profile for meaningful AG News labels)
4. TDP and TSP are notified, review, and sign
5. TDC views the **SIGNED** contract, runs local-docker training, and inspects **logs** + **provenance**
6. TDC **registers** the trained model, **deploys** it for local inference, and runs a **prediction** in the Inference app

## Demo credentials

| Role | Notes |
|---|---|
| TDC / TDP / TSP | Fresh **enterprise** users registered during the guide run (emails + orgs in screenshots) |
| Registration mode | **User Type = Enterprise** (organization field required in the UI tour) |
| Password after first login | `TestNewPassword123!` |
| Local compute | TSP is configured with **Local** cloud provider for `TRAINING_EXECUTION_MODE=local-docker` |

## Training configuration used in this tour

| Field | Value |
|---|---|
| Task / modality | Text classification (`taskType: text`) — AG News topics |
| Demo profile | **quality** (meaningful labels; use `LIFECYCLE_DEMO_QUALITY=false` for tiny/fast E2E) |
| Model | `E2E DistilBERT Quality` (`e2e-model-nlp-distilbert-quality`) |
| Architecture | `distilbert-base-uncased` |
| Framework | PyTorch |
| Privacy | Optional on quality path (DP-SGD remains on fast NLP E2E: tiny DistilBERT, `ε=0.5`) |
| Hyperparameters | `maxEpochs=2`, `batchSize=16`, `learningRate=5e-5`, `fastDevRun=false`, `trainSubsetSize=2000` |
| Dataset | Lifecycle TDP NLP catalog row with Hugging Face `ag_news` reference |
| After train | Register → Deploy → Predict (local `infer.py` / Inference app) |
| Expected demo prediction | Headline about Wall Street → **Business** |

## Happy path

### 1. Enterprise onboard TDC

Open **User Registration** and set **User Type** to **Enterprise**.
Choose role **TDC (Training Data Consumer)**, enter the organization name, public key, and register.

![Enterprise onboard TDC](screenshots/01-onboard-tdc-register.png)

### 2. TDC first login dashboard

After first-login password setup, the enterprise TDC lands on their dashboard and can browse datasets / create contracts.

![TDC first login dashboard](screenshots/02-onboard-tdc-dashboard.png)

### 3. Enterprise onboard TDP

Register an enterprise **TDP (Training Data Provider)** with organization details.
This party publishes datasets and signs contracts that use them.

![Enterprise onboard TDP](screenshots/03-onboard-tdp-register.png)

### 4. TDP first login dashboard

The TDP dashboard is the home for datasets and incoming signature requests.

![TDP first login dashboard](screenshots/04-onboard-tdp-dashboard.png)

### 5. TDP publishes an NLP dataset

The TDP publishes a **text / NLP** catalog dataset backed by the well-known Hugging Face **`ag_news`** reference.
TDCs select this dataset when creating a privacy-preserving training contract.

![TDP publishes an NLP dataset](screenshots/05-tdp-dataset-published.png)

### 6. Enterprise onboard TSP / CCRP

Register an enterprise **TSP** (Tech Service Provider; also called CCRP in older docs).
This party hosts the training environment and co-signs the contract.

![Enterprise onboard TSP / CCRP](screenshots/06-onboard-tsp-register.png)

### 7. TSP Local cloud readiness

Configure a dedicated **Local** TSP (`ccrp.e2e@test.com`) so contracts can run with `TRAINING_EXECUTION_MODE=local-docker`. Other TSPs use cloud providers (OCI/Azure/AWS).

![TSP Local cloud readiness](screenshots/07-onboard-tsp-local.png)

### 8. TDC creates contract — NLP model & dataset

The TDC selects the TDP **AG News** NLP dataset and the catalog model **DistilBERT Quality**.
Contract terms reference **PyTorch** / **DistilBERT** training for meaningful AG News inference labels.

![TDC creates contract — NLP model & dataset](screenshots/08-tdc-create-details.png)

### 9. TDC creates contract — select TSP

Select the onboarded **TSP** for the contract. Local Docker training uses `ccrpCloudProvider: Local` (static Local TSPs: `ccrp.e2e@test.com`, `tsp.local@jurisdiction-test.com`).

![TDC creates contract — select TSP](screenshots/09-tdc-create-tsp.png)

### 10. TDC creates contract — submit path

Complete environment/KMS settings and submit the wizard.
The contract enters **PENDING_TDP_APPROVAL** and notifies the TDP (then TSP after TDP signs).

![TDC creates contract — submit path](screenshots/10-tdc-create-submit.png)

### 11. TDP gets notified

The TDP sees signature / contract notifications for agreements that reference their datasets.

![TDP gets notified](screenshots/11-tdp-notifications.png)

### 12. TDP reviews and signs

On **Contract Details**, the TDP reviews terms and clicks **Sign Contract as TDP**. Status advances to **PENDING_TSP_APPROVAL**.

![TDP reviews and signs](screenshots/12-tdp-sign.png)

### 13. TSP / CCRP gets notified

The TSP is notified when a contract is waiting for clean-room provider signature.

![TSP / CCRP gets notified](screenshots/13-tsp-notifications.png)

### 14. TSP / CCRP reviews and signs

The TSP reviews environment commitments and signs. When TDP and TSP have both signed, status becomes **SIGNED**.

![TSP / CCRP reviews and signs](screenshots/14-tsp-sign.png)

### 15. TDC views the signed contract

The TDC opens the contract and confirms **SIGNED** (TDP + TSP signatures complete).

![TDC views the signed contract](screenshots/15-tdc-signed-contract.png)

### 16. TDC starts training

On **Training**, the TDC starts a job for the signed contract (`TRAINING_EXECUTION_MODE=local-docker` in local env).

![TDC starts training](screenshots/16-tdc-training-ready.png)

### 17. Training completed (PyTorch DistilBERT)

When the local trainer finishes, the job status is **COMPLETED**.
This tour uses the **quality** profile: **PyTorch** / **DistilBERT** on AG News so inference labels are meaningful for stakeholders.

![Training completed (PyTorch DistilBERT)](screenshots/17-tdc-training-completed.png)

### 18. TDC views provenance report

**View job provenance** opens the host/API provenance bundle (datasets, model architecture, privacy metrics, artifacts).

![TDC views provenance report](screenshots/18-tdc-provenance.png)

### 19. TDC views training run logs

**View logs** shows trainer/runner output (framework, architecture, DP flags) captured for the local-docker job.

![TDC views training run logs](screenshots/19-tdc-training-logs.png)

### 20. Register trained model for inference

On the completed job, expand **Results & artifact** and click **Register trained model for inference**.
This creates an **AIModel** catalog row from the training artifact (usable in new contracts and inference).

![Register trained model for inference](screenshots/20-tdc-register-model.png)

### 21. Deploy model for inference

Click **Deploy for inference** to mark the registered model as **DEPLOYED** for local prediction.
The platform points at the job’s `model.bin` artifact and records task type (here: **text**).

![Deploy model for inference](screenshots/21-tdc-deploy-inference.png)

### 22. Inference deployed — open app

After deploy, open the **Inference app** from the training job detail (or the TDC sidebar **Inference** item).

![Inference deployed — open app](screenshots/22-tdc-inference-deployed.png)

### 23. Inference app — request ready

The Inference app lists deployed models and pre-fills an example JSON request.
For this NLP tour the example is a short news headline (`{ "text": "..." }`).

![Inference app — request ready](screenshots/23-tdc-inference-app.png)

### 24. Inference app — prediction result (+ Open-GMASE gate)

**Run prediction** calls the local inferencer (`infer.py` via Docker) against the trained DistilBERT artifact.
With the **quality** profile, the default Wall Street headline predicts **Business** (AG News: World / Sports / Business / Sci/Tech).
When `GMASE_INFERENCE_GATE` is on, the **Open-GMASE policy gate** panel shows ALLOW (or DENY) before the side effect is accepted, with the Rego package and audit id.

![Inference app — prediction result (+ Open-GMASE gate)](screenshots/24-tdc-inference-predict.png)

## Related docs

- [Participant onboarding & E2E lifecycle (canonical text)](../PARTICIPANT_ONBOARDING_AND_E2E_LIFECYCLE.md)
- [Multi-model contracts guide](../multi-model-user-guide/MULTI_MODEL_USER_GUIDE.md)
- [TDC training + inference runtime](../../training/TDC_TRAINING_RUNTIME.md)
- [Per-role screenshot guides](../role-user-guides/README.md)
- [Contract signing user guide](../../features/contract-signing/CONTRACT_SIGNING_USER_GUIDE.md)
- [Top-level user guide](../../USER_GUIDE.md)

## Regenerate

```bash
# Stack must be up: backend :5001, frontend :3000, Keycloak, Docker trainer image (with infer.py)
cd frontend
BACKEND_URL=http://127.0.0.1:5001 npm run test:e2e:lifecycle-guide

# Fast path (tiny DistilBERT + fastDevRun) instead of quality demo:
# LIFECYCLE_DEMO_QUALITY=false BACKEND_URL=http://127.0.0.1:5001 npm run test:e2e:lifecycle-guide
```

Optional cleanup afterward (keeps seed users/catalog): `npm run cleanup:e2e-data` from repo root.
