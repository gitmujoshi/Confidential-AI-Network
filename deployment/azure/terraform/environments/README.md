# Runtime per-contract Terraform (CCRP training environments)

Generated at runtime by `backend/services/terraformService.js` — one subdirectory per `environmentId`.

**Do not run `terraform` here manually.** Platform baseline IaC lives in the parent directory:

- [../README.md](../README.md) — AKS, PostgreSQL, ACR platform deploy
